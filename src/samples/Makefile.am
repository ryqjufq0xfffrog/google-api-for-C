bin_PROGRAMS = sample
#sample_SOURCES = sample.c ../google.c ../google.h ../google_datatype.c ../buffers.c ../buffers.h ../shell.c ../shell.h
sample_SOURCES = sample.c ../shell.c ../shell.h

sample_CFLAGS = $(DEPS_CFLAGS)
sample_LDADD = $(DEPS_LIBS) ../libgoogle.la
